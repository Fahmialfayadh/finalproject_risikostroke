di terminal install library dulu ya gesss:

pip install -r requirements.txt

copas aja itu

kalau ada error atau gabisa install coba copas ini semua:

python3 -m venv venv
source venv/bin/activate   # Linux/Mac
venv\Scripts\activate      # Windows

pip install -r requirements.txt
